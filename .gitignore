# Nuxt
.nuxt
.output
.data
.nitro
.cache
dist

# Node
node_modules
*.log*
.pnpm-debug.log*

# 환경변수 — 인증키가 들어간다. 절대 커밋하지 않는다.
.env
.env.*
!.env.example

# 에디터
.idea
.vscode/*
!.vscode/extensions.json

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel

# Python (scripts/)
__pycache__/
*.py[cod]
.venv
venv/

# 일회용 패치 스크립트 (앞에 점이 붙은 임시 .mjs)
.*.mjs

# 공모전 접수 화면 캡처 — 팀원 실명이 찍혀 있다. 공개 저장소에 올리지 않는다.
docs/apply/1차 심사 자료 제출.png

# 기능설명서 이전 판본 백업. 최신본만 추적한다(5.6MB 중복).
docs/apply/이전자료/
