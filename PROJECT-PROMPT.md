# 안동잇다 — 프로젝트 초기 구축 지시서

이 문서를 Claude Code에 그대로 전달한다.
여기 적힌 API 응답과 데이터는 **실제 호출로 검증한 결과**다. 추측이 아니다.
검증되지 않은 항목은 "미확인"으로 표시했으니 임의로 채우지 말 것.

---

## 0. 이 서비스가 무엇인가

차 없이 안동에 온 여행자를 위한 웹 서비스다.

핵심 통찰은 하나다. **배차 간격이 길다는 것은 대기 시간이 생긴다는 뜻이고, 그 시간에 주변을 다녀올 수 있다.** 이 문장이 화면 구조의 조직 원리다. 버스 도착 시간이 화면 최상단에 오고, 그 대기 시간 안에 갈 수 있는 곳이 바로 아래에 온다.

기존 안동 서비스들은 교통(안동버스정보 앱)과 관광(안동을 담다, 안동관광)이 분리돼 있어 여행자가 앱을 오가야 했다. 안동잇다는 **교통과 관광을 한 화면에 둔다.** 이것이 유일한 차별점이므로 어떤 결정도 이 원칙을 훼손해서는 안 된다.

목표 기한: 2026년 9월 20일(1차 심사 URL 제출), 10월 28일 최종 발표.

---

## 1. 기술 스택

```
Nuxt 4 (TypeScript)
pnpm
Tailwind CSS v4
Vercel 배포
백엔드 서버 없음 · 데이터베이스 없음
```

**백엔드와 DB를 두지 않는 이유**: 원천 데이터를 소유하지 않고 공공 API를 조합하는 서비스다. 관광지가 100개 내외라 DB가 필요한 규모가 아니다. 1인 개발이고 기한이 정해져 있으므로 서버 운영에 드는 시간을 기능 완성도에 쓴다.

**Nuxt server/api를 반드시 경유한다.** 세 가지 이유가 동시에 해결된다.
1. TourAPI 인증키가 클라이언트 번들에 노출되면 안 된다
2. 안동시 버스 API와 LocgoHub API가 모두 `http`라 https 페이지에서 브라우저가 직접 호출하면 Mixed Content로 차단된다
3. 공공 API 대부분이 CORS를 허용하지 않는다

---

## 2. 환경변수 (.env)

```bash
# 한국관광공사 TourAPI (data.go.kr 발급, Decoding 키 사용)
TOUR_API_KEY=

# 카카오맵 JavaScript 키 (클라이언트 노출됨, 도메인 제한 필수)
NUXT_PUBLIC_KAKAO_MAP_KEY=
```

`.env.example`을 함께 만들고 `.env`는 `.gitignore`에 넣는다.

`nuxt.config.ts`:
```ts
runtimeConfig: {
  tourApiKey: process.env.TOUR_API_KEY,        // 서버 전용
  public: {
    kakaoMapKey: process.env.NUXT_PUBLIC_KAKAO_MAP_KEY,
  },
}
```

안동시 버스 API는 인증키가 필요 없다.

---

## 3. 외부 API — 전부 실호출 검증 완료

### 3-1. 안동시 버스정보 (인증키 불필요, http)

베이스: `http://bus.andong.go.kr/m01/s04.do`

| 용도 | 요청 | 검증 |
|---|---|---|
| 정류장 전체 목록 | `?tab=1` | ✅ |
| 정류장별 도착정보 | `?i={stationId}&tab=2` | ✅ |
| 노선 목록 | `?tab=3` | ✅ |
| 노선별 정류장 | `?i={routeId}&tab=4` | ✅ |
| 노선별 차량위치 | `?i={routeId}&tab=5` | 미확인 |

**도착정보 응답 (실제)**
```json
[{
  "routeId": 354300006,
  "routeNum": "210",
  "routeNm": "210(교보생명-하회마을)",
  "via": "교보생명 -> 하회마을",
  "stationOrd": 52,
  "predictTm": 25.0,
  "remainStation": 27,
  "plateNo": "경북70자3403"
}]
```

주의사항 네 가지.

**(1) 명세서와 실제 필드가 다르다.** 명세서엔 `rstop`, `provideType`으로 적혀 있으나 실제로는 `predictTm`(도착 예정 분), `remainStation`(남은 정류장)이 온다. **실제 응답 기준으로 타입을 정의한다.** 도착 시간 계산 로직을 만들지 말 것 — `predictTm`이 이미 분 단위로 계산되어 온다.

**(2) `via` 필드가 명세서에 없지만 실제로는 존재한다.** 기점 → 종점을 알려주므로 방향 판별에 쓴다. 여행자가 버스에서 가장 혼란스러워하는 "이게 내가 가려는 방향인가"를 해결하는 정보이므로 화면에 우선 노출한다.

**(3) 기점 정류장은 빈 배열을 반환한다.** 검증 결과: 하회 `354000701`(들어오는 편 종점)은 도착정보가 나오지만, `354002046`(나가는 편 기점)은 `[]`를 반환한다. 다가오는 차가 없으니 당연하다. **관광지에서 나가는 편은 실시간으로 잡을 수 없다.**

**(4) `predictTm`이 null로 올 수 있다.** 차량 위치 미확보 시. `remainStation` 기반 표시나 "곧 도착" 폴백을 둔다.

**노선 목록 응답 (실제)**
```json
{
  "routeId": 354300004,
  "routeNm": "112(교보생명-월영교-문화관광단지)",
  "routeNum": "112",
  "runTotCnt": 0,
  "stationCnt": 14,
  "routeLen": 7146.0,
  "stStationId": 354001808, "stStationNm": "교보생명",
  "edStationId": 354000754, "edStationNm": "리첼호텔건너",
  "stTm": null, "edTm": null, "maxInterval": null, "minInterval": null
}
```

**`runTotCnt`가 이 서비스의 숨은 핵심이다.** 현재 운행 중인 차량 수다. 0이면 금일 운행 종료 또는 미운행일이다. 도착정보가 비었을 때 두 상황을 구분하는 유일한 근거다.

```
도착정보 있음                 → "12분 후"
도착정보 없음 + runTotCnt > 0 → "접근 중인 버스 없음, 배차가 길어요"
도착정보 없음 + runTotCnt = 0 → "오늘 운행 종료" (경고)
```

`stTm`/`edTm`/`interval` 필드는 **전부 null이다.** 첫차·막차는 아래 정적 시간표에서 가져온다.

### 3-2. TourAPI 기초지자체 중심관광지 (LocgoHubTarService1)

```
http://apis.data.go.kr/B551011/LocgoHubTarService1/areaBasedList1
  ?serviceKey={TOUR_API_KEY}
  &numOfRows=100&pageNo=1
  &MobileOS=ETC&MobileApp=AndongItda&_type=json
  &baseYm=202605
  &areaCd=47&signguCd=47170
```

**관광지 데이터의 뼈대다.** 실제 방문 데이터 기반 순위(`hubRank`)를 제공한다.

```json
{
  "hubTatsNm": "안동하회마을",
  "hubCtgryLclsNm": "관광지",
  "hubCtgryMclsNm": "역사관광",
  "hubRank": "1",
  "mapX": "128.517994889252000",
  "mapY": "36.539062884929000"
}
```

- `baseYm`(YYYYMM) 필수. 매월 8일 갱신되므로 최신 월이 없으면 이전 월로 폴백하는 로직 필요
- `hubCtgryLclsNm === "숙박"`을 필터링한다(100개 중 약 35개). 호텔·모텔이 관광지 순위에 섞이면 신뢰도가 떨어진다
- 상위: 1위 하회마을, 2위 월영교, 3위 병산서원, 4위 도산서원, 5위 봉정사, 7위 안동역, 21위 안동구시장, 30위 임청각

### 3-3. TourAPI 국문 관광정보 (KorService2)

```
https://apis.data.go.kr/B551011/KorService2/{operation}
  ?serviceKey={TOUR_API_KEY}
  &MobileOS=ETC&MobileApp=AndongItda&_type=json
  &numOfRows=100&pageNo=1
  &areaCode=35&sigunguCode=11
```

**상세 정보 보강용이다.** 설명·이미지·주소를 제공한다.

| contentTypeId | 분류 | 안동 건수 |
|---|---|---|
| 12 | 관광지 | 64 |
| 14 | 문화시설 | 12 |
| 15 | 축제 | **0** |
| 39 | 음식점 | 16 |

주의사항 세 가지.

**(1) 지역코드 체계가 LocgoHub와 완전히 다르다.**

| API | 파라미터명 | 안동 값 |
|---|---|---|
| KorService2 | `areaCode` / `sigunguCode` | `35` / `11` |
| LocgoHubTarService1 | `areaCd` / `signguCd` | `47` / `47170` |

파라미터 이름과 값이 둘 다 다르다. 상수로 분리 관리한다.

**(2) 이 데이터셋에 하회마을·도산서원·월영교가 없다.** 서원·종택 위주의 64건이라 안동 대표 관광지가 빠져 있다. 그래서 LocgoHub를 뼈대로 삼는다.

**(3) `searchKeyword2`의 파라미터명은 `Keyword`(대문자 K)다.** 소문자로 보내면 `INVALID_REQUEST_PARAMETER_ERROR(Keyword)`가 뜬다. 에러 메시지 괄호 안에 올바른 파라미터명이 표시되는 것이 이 API의 특징이다.

**(4) `firstimage`가 빈 값인 항목이 있다.** 관광지 64건 중 4건, 음식점 16건 중 7건(44%). 폴백 UI가 필수다.

### 3-4. 두 API의 조인

공통 키가 없다. LocgoHub는 `hubTatsCd`(해시), KorService2는 `contentid`를 쓴다.

**좌표 + 이름으로 매칭한다.**
1. 거리 200m 이내 AND 이름 유사도 임계값 이상 → 동일 장소
2. 매칭 성공 → 순위 + 상세정보 병합
3. 매칭 실패 → LocgoHub 단독 표시, 이미지 폴백

실제로 겹치는 예: 안동시립박물관(25위 ↔ 129772), 이육사문학관(36위 ↔ 2721929), 하회세계탈박물관(59위 ↔ 130150), 체화정(86위 ↔ 1621549).

---

## 4. 정적 데이터 (`/data`)

공공 API가 제공하지 않아 직접 만든 데이터다. 심사 요건상 "한국관광공사 OpenAPI 활용"은 3-2·3-3으로 충족했고, 아래는 자체 가공 데이터이므로 정적 관리해도 무방하다.

### 4-1. `data/spot-station-map.json`

관광지 ↔ 정류장ID ↔ 노선ID 매핑. **전부 실호출로 검증한 값이다.**

| 관광지 | 순위 | 들어오는 편 정류장 | 노선 | 나가는 편 노선 |
|---|---|---|---|---|
| 안동하회마을 | 1 | 354000701 | 210 / 354300006 | 354300008 |
| 월영교 | 2 | **354000716** | 112 / 354300004 | 354300005 |
| 병산서원 | 3 | 354002027 | 210 / 354300007 | 354300009 |
| 도산서원 | 4 | 354001832 | 급행3 / 354300386 | 354300387 |
| 봉정사 | 5 | 354002024 | 310 / 354300060 | 354300063 |
| 만휴정 | 9 | 354002164 | 610 / 354300204 | 354300214 |
| 임청각·칠층전탑 | 30 | 354000064(법흥동) | 112 / 354300004 | — |

**같은 이름의 정류장이 방향별로 다른 ID를 갖는다.** 하회보건진료소가 354000701(들어오는 편)과 354002046(나가는 편)으로 나뉘어 있다. 나가는 편 ID로 도착정보를 조회하면 빈 배열이 온다.

미확인 항목(`pending`): 이육사문학관, 고산정, 농암종택, 선성수상길, 부용대, 안동군자마을, 안동학가산온천. 해당 노선의 `tab=4`로 좌표 매칭하여 채운다. **임의로 채우지 말 것.**

### 4-2. `data/timetable-spots.json` · `timetable-raw.json`

안동시 공공데이터포털 운행시간표 엑셀(4개 파일, 42개 시트, 166개 정류장 컬럼)을 파싱한 결과다. 시트마다 컬럼 구조가 달라 완전 파싱은 포기하고 **첫차·막차·운행횟수만 수확**했다.

| 관광지 | 가는 편 | 돌아오는 편 |
|---|---|---|
| 하회마을 | 교보건너 06:30~18:20 (13회), 도착 07:35~19:10 | **시간표 없음** |
| 병산서원 | 도착 09:30~15:30 (**3회**) | 시간표 없음 |
| 봉정사 | 06:55~19:25 (6회) | 시간표 없음 |
| 도산서원 | 06:10~19:50 (7회) | 온혜발 07:50~**20:40** ✅ |
| 월영교 | 08:25~**18:45** (7회) | 시간표 없음 |
| 만휴정 | 05:50~20:15 | 길안발 06:20~**20:00** (38회) ✅ |

**공식 시간표는 안동 기준으로만 작성되어 있다.** 시내 출발과 관광지 도착 시각만 적고 회차 후 출발 시각은 대부분 생략돼 있다. 생활 노선(610 길안, 611 임동, 512 도산, 임하)에는 "○○에서 안동출발" 컬럼이 있으나, 정작 관광 노선(210 하회, 310 봉정사, 112 안동댐)에는 없다.

**추정치를 막차처럼 표시하지 말 것.** 부정확한 교통 정보는 여행자를 정류장에 세워둔다. 관측 가능한 사실만 말한다.

두 가지 위험 정보를 화면에서 강하게 경고해야 한다.

- **병산서원**: 하루 3회, 마지막 도착 15:30. 오후 늦게 출발하면 방문 불가
- **월영교**: 야경 명소인데 막차 18:45. 조명이 켜질 무렵 버스가 끊긴다

---

## 5. 내부 API 설계 (`server/api`)

```
GET /api/spots                      중심관광지 순위 (숙박 제외, 상세 병합)  캐시 1d
GET /api/spots/[id]                 관광지 상세                          캐시 1d
GET /api/spots/nearby?lat=&lng=     현 위치 주변                         캐시 짧게
GET /api/food                       음식점 목록                          캐시 6h
GET /api/bus/arrivals?stationId=    실시간 도착 (via 방향 포함)            캐시 없음
GET /api/bus/routes                 노선 목록 (runTotCnt 포함)            캐시 1m
GET /api/bus/stations               정류장 목록                          캐시 1d
GET /api/spot-bus/[spot]            관광지 통합 버스 정보                  캐시 없음
```

`/api/spot-bus/[spot]`이 이 서비스의 핵심 엔드포인트다. 세 소스를 결합해 반환한다.

```ts
{
  inbound:  { predictTm, remainStation, routeNum, via },  // 실시간
  outbound: { hasRealtime: false, reason: '기점 정류장' },  // 항상 없음
  schedule: { departFirst, departLast, arriveLast, runs }, // 정적 시간표
  service:  { runTotCnt, isOperating },                    // 운행 여부
  warning:  string | null                                  // 병산서원·월영교 등
}
```

---

## 6. 화면 구조

3개 탭 + 상세. 모바일 하단 탭, 데스크톱 상단 네비.

```
[지금 여기]  현 위치 · 버스 · 도보권
[둘러보기]   검색 · 관광지/식도락 2탭 · 카드 그리드
[걷는 길]    낮·밤 도보 코스
  └ 상세     관광지 정보 + 여기 가는 버스
```

### 지금 여기 (홈)

**순서가 곧 이 서비스의 주장이다. 바꾸지 말 것.**

1. 위치 헤더
2. **버스 카드** — 다음 버스 남은 분을 64px로 크게. 이 화면에서 유일하게 큰 타이포
3. **대기 시간 밴드** — "걸어서 7분 안에". 지도(도보 반경 원) 먼저, 그 아래 가로 스크롤 카드
4. 인기 관광지 순위 (데스크톱에서는 우측 sticky)

### 둘러보기

검색 입력(pill) → 관광지/식도락 2탭 → 카테고리 칩 → 정렬·목록/지도 토글 → 카드 그리드.

식도락 카테고리는 **찜닭 / 헛제삿밥 / 간고등어·한식 / 카페·베이커리**. 기획서에 명시한 안동 향토음식 분류다.

### 관광지 상세

**정보와 버스를 한 화면에 둔다.** 이것이 차별점이다. 갤러리 → 설명 → 가는 방법(버스) → 근처 함께 볼 곳. 데스크톱은 우측 sticky 버스 카드, 모바일은 하단 고정 바.

---

## 7. 디자인 시스템

에어비앤비 디자인 언어를 따르되 브랜드 색만 안동잇다 고유값을 쓴다.

```css
--primary: #D9453C;        /* 단청 주홍 — 유일한 고전압 색 */
--primary-active: #B8322A;
--ink: #222222; --body: #3f3f3f; --muted: #6a6a6a;
--hairline: #dddddd; --hairline-soft: #ebebeb;
--canvas: #ffffff; --surface-soft: #f7f7f7; --surface-strong: #f2f2f2;

--r-xs:4px --r-sm:8px --r-md:14px --r-lg:20px --r-xl:32px --r-full:9999px
--s-xs:4px --s-sm:8px --s-md:12px --s-base:16px --s-lg:24px --s-xl:32px

/* 그림자는 단 하나의 정의만 존재한다. 단계별 elevation을 만들지 말 것 */
--float: rgba(0,0,0,.02) 0 0 0 1px, rgba(0,0,0,.04) 0 2px 6px 0, rgba(0,0,0,.1) 0 4px 8px 0;
```

폰트는 Pretendard(Inter의 한글 대응). 타이포 무게를 낮게 유지한다 — display 22px/500, 제목 16px/600, 본문 16px/400. **64px/700은 버스 도착 시간에만 쓴다.** 시스템에서 유일하게 큰 소리를 내는 자리다.

브레이크포인트: `744px` / `1128px` / `1440px`. **행을 리플로우하지 말고 열 수만 줄인다.**

**주의**: 카드 전체를 클릭 가능하게 만들 때 `<button>`으로 감싸면 내부에 `<div>`를 넣을 수 없어 `<span>`을 쓰게 되는데, span은 기본 `display:inline`이라 `aspect-ratio`가 적용되지 않아 레이아웃이 무너진다. `<NuxtLink>` 또는 `<div role="button" tabindex="0">`으로 감싸는 편이 낫다.

---

## 8. 기획서 대비 변경 사항 (기록 필수)

| 기획 | 실제 | 사유 |
|---|---|---|
| 남은 정류장 수로 도착 시간 계산 | `predictTm` 그대로 사용 | API가 이미 분 단위로 제공 |
| 막차 안내 | 실시간 + `runTotCnt` + 정적 시간표 3단 결합 | API에 시간표 필드가 null이나 노선 목록으로 운행 여부 판별 가능 |
| 집중률 API로 인기 순위 | LocgoHub `hubRank` | 방문 데이터 기반이라 더 직접적 |
| 축제·행사 연동 | 제외 | KorService2 안동 축제 `totalCount: 0` |
| 낙강물길공원 야간 코스 | 월영교 중심으로 재구성 | 2028년 12월까지 공사 중 |

1차 심사 제출 서류의 활용 API 목록을 실제 사용분으로 정정할 것.

---

## 9. 문서화 규칙

`docs/decisions.md`와 `docs/dev-log.md`를 유지한다. 이 프로젝트는 포트폴리오를 겸하므로 **"왜 그렇게 했는가"의 기록이 코드만큼 중요하다.**

특히 다음과 같은 판단은 반드시 남긴다.
- 명세서와 실제 응답이 달랐던 사례
- 기능을 제외하거나 방식을 바꾼 사유
- 데이터 한계를 UI로 대응한 방법

---

## 10. 첫 작업 순서

1. Nuxt 4 + TypeScript + Tailwind v4 프로젝트 초기화, pnpm
2. `.env.example`, `nuxt.config.ts` runtimeConfig, `.gitignore`
3. `types/` — 실제 응답 기준 타입 정의 (버스 도착, 노선, LocgoHub, KorService2)
4. `constants/region.ts` — API별 지역코드 분리
5. `server/api/bus/*` — 프록시 3종 + 캐싱
6. `server/api/spots/*` — LocgoHub 조회, 숙박 필터, KorService2 조인
7. `data/*.json` 배치, 로더 컴포저블
8. `/api/spot-bus/[spot]` 통합 엔드포인트
9. UI 컴포넌트 — 디자인 토큰부터
10. 3개 화면 + 상세

**각 단계마다 실제 API를 호출해 응답을 확인하고 진행할 것.** 이 프로젝트에서 반복해 확인된 사실은 하나다 — **문서보다 실물이 정확하다.**
