<template>
  <!--
    페이지 전환 표시 — 눌렸다는 사실을 화면 맨 위에 남긴다

    관광지 상세는 데이터가 다 온 뒤에야 그려진다(→ `pages/spots/[id].vue`).
    그 전까지 브라우저는 **이전 화면을 그대로 들고 있어서**, 누른 사람에게는
    눌리지 않은 것과 구별되지 않는다. 실측(2026-09-16): 목록에서 상세까지 약 0.5초,
    콜드스타트에는 그 이상. 그동안 화면에는 아무 변화가 없었다.

    Nuxt가 들고 있는 컴포넌트지만 **직접 놓아야 나타난다.** 기본으로 켜져 있지 않다.

    색은 primary(단청 주홍)다. 이 서비스에서 고전압 색은 하나뿐이고(→ main.css),
    "지금 무언가 진행 중"은 그 하나를 쓸 자리다.
  -->
  <NuxtLoadingIndicator color="#d9453c" />

  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
</template>
